{
  "name": "vroom-poc-server",
  "version": "1.0.0",
  "description": "Server that uses Distance matrix api to provide custom matrix to Vroom service.",
  "main": "app.js",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
